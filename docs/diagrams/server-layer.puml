@startuml server-layer
!theme plain
skinparam backgroundColor white
skinparam componentStyle rectangle
skinparam linetype ortho
skinparam packageBackgroundColor #fff5e8
skinparam componentBackgroundColor #ffffff
skinparam ArrowColor #555555

title Server Layer (FastAPI + SQLModel)

[SDK exporter] as SDK

package "Server Layer" #fff5e8 {
  [Ingest\n/ingest] as Ingest
  database "SQLite" as DB
  [Eval Orchestrator\nMode 1 + 3] as EvalOrch
  [Regression Driver\nMode 2] as RegOrch
  [Drift Workers\nADWIN · MMD · PSI\n+ Attribution-PSI] as Drift
  [REST API\n/runs · /evals · /drift\n/datasets · /regressions\n/compare] as Api
}

[Frontend  pages] as Web

SDK --> Ingest : HTTP POST
Ingest --> DB : write spans + run
Ingest ..> EvalOrch : background tasks
EvalOrch --> DB : write EvalResult
RegOrch --> DB : write RegressionRun
Drift --> DB : poll evals + spans\n(5 s tick)
Api <-- DB

Web --> Api : REST

@enduml
